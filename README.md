# Minimal repro for eslint-plugin-import-x / unrs-resolver issue

Issue: https://github.com/un-ts/eslint-plugin-import-x/issues/486

Note: if you switch to the branch with the earlier version of unrs-resolver, you need to `rm -rf node_modules && pnpm i` in order for unrs-resolver to be properly downgraded to 1.11.1
